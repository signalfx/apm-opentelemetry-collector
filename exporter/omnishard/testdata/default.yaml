receivers:
  examplereceiver:

exporters:
  omnishard:

processors:
  exampleprocessor:

service:
  pipelines:
    traces:
      receivers: [examplereceiver]
      processors: [exampleprocessor]
      exporters: [omnishard]


