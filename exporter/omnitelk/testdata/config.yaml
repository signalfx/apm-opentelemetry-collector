receivers:
  examplereceiver:
    enabled: true

exporters:
  omnitelk:
    enabled: true
    endpoint: 0.0.0.0:1234
    streams: 456

processors:
  exampleprocessor:
    enabled: true

pipelines:
  traces:
    receivers: [examplereceiver]
    processors: [exampleprocessor]
    exporters: [omnitelk]
